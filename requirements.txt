# Billy Mitchell — runtime deps (Python 3.10–3.14; a venv is recommended).
stable-retro>=1.0.1   # in-process libretro NES emulator (RAM access + state cloning)
requests>=2.31        # LM Studio OpenAI-compatible HTTP client
numpy>=1.24           # action vectors + fast cosine (pulled in by stable-retro anyway)
